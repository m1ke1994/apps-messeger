services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    volumes:
      - backend_db:/data
    restart: unless-stopped

volumes:
  backend_db:
    driver: local
